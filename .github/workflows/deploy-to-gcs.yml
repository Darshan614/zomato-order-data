name: Upload PySpark Job to GCS

on:
  push:
    branches:
      - stage
      - main
  workflow_dispatch:

jobs:
  upload-to-gcs:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set ENV
        id: set-env
        run: |
          echo "⚙️ GITHUB_REF: $GITHUB_REF"
          echo "⚙️ Branch name extracted: ${GITHUB_REF##*/}"
          if [[ "${GITHUB_REF##*/}" == "stage" ]]; then
            echo "ENV=STG" >> $GITHUB_ENV
          elif [[ "${GITHUB_REF##*/}" == "main" ]]; then
            echo "ENV=PROD" >> $GITHUB_ENV
          else
            echo "❌ Unknown branch"
            exit 1
          fi

      - name: Export dynamic secrets to ENV
        run: |
          echo "GCP_PROJECT=${{ secrets.GCP_PROJECT_STG }}" >> $GITHUB_ENV
          echo "GCS_BUCKET=${{ secrets.GCS_BUCKET_STG }}" >> $GITHUB_ENV
        if: env.ENV == 'STG'

      - name: Export dynamic secrets to ENV
        run: |
          echo "GCP_PROJECT=${{ secrets.GCP_PROJECT_PROD }}" >> $GITHUB_ENV
          echo "GCS_BUCKET=${{ secrets.GCS_BUCKET_PROD }}" >> $GITHUB_ENV
        if: env.ENV == 'PROD'

      - name: Authenticate to GCP (STG)
        uses: google-github-actions/auth@v1
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY_STG }}
        if: env.ENV == 'STG'

      - name: Authenticate to GCP (PROD)
        uses: google-github-actions/auth@v1
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY_PROD }}
        if: env.ENV == 'PROD'

      - name: Set up gcloud CLI
        uses: google-github-actions/setup-gcloud@v1
        with:
          project_id: ${{ env.GCP_PROJECT }}

      - name: Check if dependencies changed
        id: deps-changed
        run: |
          git fetch origin ${{ github.event.before }}
          changed_files=$(git diff --name-only ${{ github.event.before }} ${{ github.sha }})
          echo "🔍 Changed files: $changed_files"
          
          # Check for dependency changes
          if echo "$changed_files" | grep -qE 'mssql_to_bq/scripts/dependencies/(logging|spark)\.py'; then
            echo "zip_needed=true" >> $GITHUB_ENV
          else
            echo "zip_needed=false" >> $GITHUB_ENV
          fi

          # Check for df-avro-pq changes
          if echo "$changed_files" | grep -qE '^mssql_to_bq/scripts/df-avro-pq/'; then
            echo "df_avro_pq_changed=true" >> $GITHUB_ENV
            echo "df_avro_pq_files=$(echo "$changed_files" | grep '^mssql_to_bq/scripts/df-avro-pq/')" >> $GITHUB_ENV
          else
            echo "df_avro_pq_changed=false" >> $GITHUB_ENV
          fi

          # Check for zomato-composer DAG changes
          if echo "$changed_files" | grep -qE '^zomato-composer/'; then
            echo "composer_dag_changed=true" >> $GITHUB_ENV
            echo "composer_dag_files=$(echo "$changed_files" | grep '^zomato-composer/')" >> $GITHUB_ENV
          else
            echo "composer_dag_changed=false" >> $GITHUB_ENV
          fi

      - name: Zip dependencies
        if: env.zip_needed == 'true'
        run: |
          echo "📦 Creating dependencies.zip..."
          cd mssql_to_bq/scripts
          zip -r dependencies.zip dependencies
          cd -

      - name: Upload PySpark job and zipped dependencies
        run: |
          echo "✅ Uploading to GCS Bucket: $GCS_BUCKET"
          gsutil -m cp mssql_to_bq/scripts/sqlserver_to_bq_job.py gs://${{ env.GCS_BUCKET }}/pyspark/
          if [[ "${{ env.zip_needed }}" == "true" ]]; then
            gsutil cp mssql_to_bq/scripts/dependencies.zip gs://${{ env.GCS_BUCKET }}/pyspark/
          else
            echo "🛑 No changes in dependencies/*.py — skipping zip upload"
          fi

      - name: Upload df-avro-pq files to GCS
        if: env.df_avro_pq_changed == 'true'
        run: |
          echo "✅ Uploading df-avro-pq files to GCS Bucket: df-avro-pq-bk"
          for file in ${{ env.df_avro_pq_files }}; do
            gsutil cp "$file" gs://df-avro-pq-bk/
          done

      - name: Upload Composer DAGs to Composer bucket
        if: env.composer_dag_changed == 'true'
        run: |
          echo "✅ Uploading Airflow DAG files to Composer Bucket"
          for file in ${{ env.composer_dag_files }}; do
            gsutil cp "$file" gs://us-central1-zomato-comp-env-66d048dc-bucket/dags/
          done
